version: "3.2"
services:
  node:
    image: jmask-node
    environment:
      UID: ${uid}
      GID: ${gid}
      PORT_NODE: ${port_node}
      PORT_KARMA: ${port_karma}
    ports:
      - "${port_node}:8080"
      - "${port_karma}:9876"
    volumes:
      - type: bind
        source: .
        target: /var/www/jmask
